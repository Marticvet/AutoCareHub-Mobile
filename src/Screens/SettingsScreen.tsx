import {
    View,
    Text,
    TextInput,
    StyleSheet,
    TouchableOpacity,
    FlatList,
    ScrollView,
} from "react-native";
import { useAuth } from "../providers/AuthProvider";

function SettingsScreen() {

    return(
        <View>
            <Text> ebanie</Text>
        </View>
    )
}

export default SettingsScreen;